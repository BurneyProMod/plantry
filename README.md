# plantry
A meal planner/recipe book that minimizes ingrediant waste

# Dependencies
- gradle
# Structure
plantry/
├── core/           ← domain classes, algorithms, DTOs
├── backend/        ← REST API, persistence, service wiring
└── app/            ← Kotlin UI, Retrofit clients, Room models

# Roadmap
- Gradle
